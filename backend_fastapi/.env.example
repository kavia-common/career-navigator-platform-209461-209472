# Backend FastAPI environment configuration for Career Navigator Platform

# SQLite connection URL. For local dev, a file in project root:
# Examples:
#   sqlite:///./career_nav.db         (relative file)
#   sqlite:////data/career_nav.db     (absolute, useful in Docker with volume)
DATABASE_URL=sqlite:///./career_nav.db

# LLM recommendation microservice endpoint (backend will POST here for /recommend)
# Update host/port to match deployment (e.g., docker-compose service name or k8s service DNS)
LLM_SERVICE_URL=http://localhost:8001/recommend

# CORS allowed origins:
# - Use * to allow all origins (dev)
# - Or provide comma-separated list of origins (e.g., http://localhost:3000,https://your.site)
CORS_ORIGINS=*

# Uvicorn host/port (used by Dockerfile's default CMD)
UVICORN_HOST=0.0.0.0
UVICORN_PORT=8000
