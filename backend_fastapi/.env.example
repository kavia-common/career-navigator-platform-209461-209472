# Backend FastAPI environment configuration

# SQLite DB URL (use absolute or relative path). Example uses local file.
DATABASE_URL=sqlite:///./career_nav.db

# LLM recommendation microservice endpoint for proxying /recommend
LLM_SERVICE_URL=http://localhost:8001/recommend

# Comma-separated CORS origins allowed to access the API (or '*' for all)
CORS_ORIGINS=http://localhost:3000
